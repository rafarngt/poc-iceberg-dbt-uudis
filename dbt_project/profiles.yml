iceberg_spark:
  target: dev
  outputs:
    dev:
      type: spark
      method: thrift
      host: spark-thrift       # nombre del servicio en docker-compose
      port: 10000
      user: sparkuser
      auth: NOSASL
      schema: bronze            # namespace que existe en iceberg
      connect_retries: 5
      connect_timeout: 60
      threads: 4
